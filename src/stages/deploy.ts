/**
 * deploy — STUB. Publica linktree/web y guarda las URLs en generated.
 * Setea status "deployed". Aun no implementado.
 */
export async function deploy(_slug: string): Promise<never> {
  throw new Error("deploy: no implementado");
}
