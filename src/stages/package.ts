/**
 * package — STUB. Arma el mensaje de outreach listo para copiar/pegar a WhatsApp.
 * Setea status "packaged". Aun no implementado.
 */
export async function pkg(_slug: string): Promise<never> {
  throw new Error("package: no implementado");
}
