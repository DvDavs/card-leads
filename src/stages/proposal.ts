/**
 * proposal — STUB. Genera proposal.md con ideas de sistemas por rubro.
 * Setea status "proposal_ready" y ESPERA OK. Aun no implementado.
 */
export async function proposal(_slug: string): Promise<never> {
  throw new Error("proposal: no implementado");
}
