/**
 * build-web — STUB. Rellena el template por rubro para generar el sitio completo.
 * Setea status "web_built". Aun no implementado.
 */
export async function buildWeb(_slug: string): Promise<never> {
  throw new Error("build-web: no implementado");
}
