/**
 * extract — STUB. LLM de vision -> JSON (business/contact/brand desde la tarjeta).
 * Setea status "extracted" y ESPERA revision humana. Aun no implementado.
 */
export async function extract(_slug: string): Promise<never> {
  throw new Error("extract: no implementado (etapa LLM pendiente)");
}
